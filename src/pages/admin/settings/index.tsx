import React from 'react';
import styled from 'styled-components';
import { MainContainer } from '../../../components/styled';

// Settings such as hiding features, hiding products until they are ready.
export const Settings = () => <SettingsMain>Here</SettingsMain>;

const SettingsMain = styled(MainContainer)`
    flex-direction: column;
`;
